<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>TODO</title>
    <link rel="stylesheet" href="css/style.css">
  </head>
  <body>
    <div class="table-container">
      <h2 class="title">Update ToDo</h2>

      <div>
        <a href="/" class="btn">Back</a>

        <form action="/update-task/<%= task._id %>" method="post">
          <div class="update-task">
            <div class="form-group">
              <label for="task" class="text">Task</label>
              <input
                type="text"
                name="task"
                value="<%= task.task %>"
                placeholder="Type Todo Task"
              />
            </div>

            <div class="form-group">
                <label for="desc" class="text">Description</label>
                <input type="text" name="desc" value="<%= task.desc %>" placeholder="Type description here"/>
            </div>

            <div class="form-group">
               <button class="btn" type="submit">Save</button>
            </div>

          </div>
        </form>
      </div>
    </div>
  </body>
</html>
